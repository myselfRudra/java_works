package queue;

import java.util.Queue;
import java.util.Stack;

public class reverse_quek {
    public Queue<Integer> modifyQueue(Queue<Integer> q,int k)
    {
        Stack<Integer> st = new Stack<>();
        int n = q.size()-k;
        while(k++ )
    }
    
}
